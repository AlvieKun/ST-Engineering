'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { createSupabaseBrowserClient } from '@/lib/supabase/client';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{if(!process.env.NEXT_PUBLIC_SUPABASE_URL||(!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY&&!process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)){setError('Supabase browser configuration is incomplete. Add the public Supabase key to .env.local and restart the dev server.');return}const {data,error}=await createSupabaseBrowserClient().auth.signInWithPassword({email:email.trim(),password});if(error){setError(error.message);return}if(!data.user){setError('Sign-in did not return a user session.');return}if(process.env.NEXT_PUBLIC_ADMIN_USER_ID&&data.user.id!==process.env.NEXT_PUBLIC_ADMIN_USER_ID){await createSupabaseBrowserClient().auth.signOut();setError('This account is not authorized for the admin panel.');return}router.push('/admin');router.refresh()}catch{setError('Unable to reach Supabase. Check the public URL/key configuration and restart the app.')}finally{setLoading(false)}}return <main className="container flex min-h-[70vh] max-w-xl items-center py-20"><div className="w-full"><p className="eyebrow mb-5">Private workspace / Authentication</p><h1 className="title">Welcome back.</h1><p className="mt-6 text-[var(--muted)]">Sign in with the portfolio’s Supabase account.</p><form onSubmit={submit} className="mt-10 space-y-6"><label className="block"><span className="mb-2 block font-mono text-xs text-[var(--muted)]">Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full border-b grid-line bg-transparent py-3 outline-none focus:border-[var(--accent)]"/></label><label className="block"><span className="mb-2 block font-mono text-xs text-[var(--muted)]">Password</span><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} className="w-full border-b grid-line bg-transparent py-3 outline-none focus:border-[var(--accent)]"/></label>{error&&<p role="alert" className="border-l-2 border-[var(--accent)] pl-4 text-sm text-[var(--accent)]">{error}</p>}<button disabled={loading} style={{color:'var(--paper)',backgroundColor:'var(--ink)'}} className="px-5 py-3 font-mono text-xs disabled:opacity-50">{loading?'Signing in…':'Sign in'}</button></form></div></main>}
