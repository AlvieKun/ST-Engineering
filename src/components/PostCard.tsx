import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import type { Post } from '@/lib/data';
export default function PostCard({post}:{post:Post}){return <Link href={`/blog/${post.slug}`} className="group block border-t grid-line py-5"><div className="grid gap-2 md:grid-cols-[150px_1fr_100px] md:items-start"><div className="font-mono text-xs text-[var(--muted)]">{post.date}</div><div><h3 className="text-xl font-bold tracking-tight group-hover:text-[var(--accent)]">{post.title}</h3><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">{post.excerpt}</p><div className="mt-3 flex flex-wrap gap-2">{post.tags.map(t=><span key={t} className="font-mono text-[10px] text-[var(--accent)]">#{t}</span>)}</div></div><div className="flex items-center justify-between font-mono text-xs text-[var(--muted)] md:justify-end md:gap-3">{post.readTime}<ArrowUpRight size={15}/></div></div></Link>}
