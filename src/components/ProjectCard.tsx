import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import type { Project } from '@/lib/data';
export default function ProjectCard({project,index=0}:{project:Project;index?:number}){return <Link href={`/projects/${project.slug}`} className="group block border-t grid-line py-6"><div className="grid gap-4 md:grid-cols-[80px_1fr_1fr_auto] md:items-start"><span className="font-mono text-xs text-[var(--muted)]">0{index+1}</span><div><div className="mb-3 flex items-center gap-3"><span className="eyebrow">{project.category}</span><span className="font-mono text-[11px] text-[var(--muted)]">{project.status}</span></div><h3 className="text-2xl font-bold tracking-tight group-hover:text-[var(--accent)]">{project.title}</h3></div><p className="max-w-md text-sm leading-6 text-[var(--muted)]">{project.summary}</p><ArrowUpRight className="text-[var(--accent)] transition-transform group-hover:translate-x-1 group-hover:-translate-y-1"/></div><div className="mt-5 ml-0 flex flex-wrap gap-2 md:ml-[80px]">{project.tags.map(t=><span key={t} className="rounded-full border grid-line px-2.5 py-1 font-mono text-[10px] text-[var(--muted)]">{t}</span>)}</div></Link>}
