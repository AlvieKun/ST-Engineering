'use client'; import { useRouter } from 'next/navigation'; import { createSupabaseBrowserClient } from '@/lib/supabase/client'; export default function AdminLogout(){const router=useRouter();return <button onClick={async()=>{await createSupabaseBrowserClient().auth.signOut();router.push('/admin/login')}} className="font-mono text-xs text-[var(--muted)] hover:text-[var(--accent)]">Log out</button>}
